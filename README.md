To run the program, you need to enter the following installation command in the terminal to install the corresponding Python library.
pip install numpy pandas matplotlib seaborn scipy statsmodels pingouin
